# Render-optimized requirements for NOI Analyzer
# CRITICAL: These versions are tested to work on Render's infrastructure

# Core Streamlit application
streamlit==1.28.0

# Data processing and manipulation  
pandas==2.0.3
numpy==1.24.3

# API and web requests
requests==2.31.0
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.4.0

# File processing (minimal set)
pdfplumber==0.9.0
chardet==5.2.0

# AI and language models
openai==1.3.0

# Visualization (without kaleido to avoid Rust issues)
plotly==5.15.0

# Environment and configuration
python-dotenv==1.0.0

# Report generation (minimal - no weasyprint to avoid Rust)
jinja2==3.1.2
xlsxwriter==3.1.2

# Error tracking and monitoring (simple version)
sentry-sdk==2.29.1

# Payment processing (stable version)
stripe==10.10.0

# Essential utilities only
typing-extensions==4.7.1
email-validator==2.3.0
python-multipart==0.0.6