# ABSOLUTE MINIMAL - Only 4 essential packages
fastapi==0.104.1
uvicorn==0.24.0
stripe==6.8.0
python-dotenv==1.0.0

# Environment
python-dotenv==1.0.0

# Database (using sqlite3 which is built-in to Python)
# No SQLAlchemy or other ORMs to avoid dependencies

# That's it - absolutely minimal set! 